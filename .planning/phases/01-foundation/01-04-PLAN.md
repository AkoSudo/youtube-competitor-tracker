---
phase: 01-foundation
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/App.tsx
  - src/pages/ChannelsPage.tsx
  - src/pages/IdeasPage.tsx
  - src/components/Nav.tsx
autonomous: true

must_haves:
  truths:
    - "User can navigate between Channels and Ideas pages"
    - "Current page is highlighted in navigation"
    - "Navigation is visible on all pages"
    - "Ideas shows count badge (0 for now)"
  artifacts:
    - path: "src/App.tsx"
      provides: "Router configuration"
      contains: "BrowserRouter"
    - path: "src/components/Nav.tsx"
      provides: "Navigation component"
      exports: ["Nav"]
      min_lines: 20
    - path: "src/pages/ChannelsPage.tsx"
      provides: "Channels page shell"
      exports: ["ChannelsPage"]
    - path: "src/pages/IdeasPage.tsx"
      provides: "Ideas page placeholder"
      exports: ["IdeasPage"]
  key_links:
    - from: "src/App.tsx"
      to: "src/pages/ChannelsPage.tsx"
      via: "Route element"
      pattern: "Route.*ChannelsPage"
    - from: "src/components/Nav.tsx"
      to: "react-router"
      via: "NavLink import"
      pattern: "NavLink"
---

<objective>
Set up React Router with navigation between Channels and Ideas pages.

Purpose: REQ-UI-006 requires navigation structure with Channels page and Ideas page with count badge.
Output: Working navigation with highlighted active state, page shells ready for content.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md (if exists)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Navigation Component</name>
  <files>
    src/components/Nav.tsx
  </files>
  <action>
    Create src/components directory if not exists.

    Create src/components/Nav.tsx with YouTube-style navigation:
    ```typescript
    import { NavLink } from 'react-router'

    interface NavProps {
      ideasCount?: number
    }

    export function Nav({ ideasCount = 0 }: NavProps) {
      return (
        <nav className="bg-[#272727] border-b border-[#3f3f3f]">
          <div className="max-w-7xl mx-auto px-4">
            <div className="flex items-center h-14 gap-8">
              {/* Logo/Brand */}
              <div className="flex items-center gap-2">
                <svg
                  className="w-8 h-8 text-red-600"
                  viewBox="0 0 24 24"
                  fill="currentColor"
                >
                  <path d="M19.615 3.184c-3.604-.246-11.631-.245-15.23 0-3.897.266-4.356 2.62-4.385 8.816.029 6.185.484 8.549 4.385 8.816 3.6.245 11.626.246 15.23 0 3.897-.266 4.356-2.62 4.385-8.816-.029-6.185-.484-8.549-4.385-8.816zm-10.615 12.816v-8l8 3.993-8 4.007z" />
                </svg>
                <span className="font-semibold text-lg hidden sm:block">
                  Competitor Tracker
                </span>
              </div>

              {/* Navigation Links */}
              <div className="flex items-center gap-1">
                <NavLink
                  to="/"
                  className={({ isActive }) =>
                    `px-4 py-2 rounded-lg text-sm font-medium transition-colors ${
                      isActive
                        ? 'bg-[#3f3f3f] text-white'
                        : 'text-[#aaaaaa] hover:bg-[#3f3f3f] hover:text-white'
                    }`
                  }
                >
                  Channels
                </NavLink>

                <NavLink
                  to="/ideas"
                  className={({ isActive }) =>
                    `px-4 py-2 rounded-lg text-sm font-medium transition-colors flex items-center gap-2 ${
                      isActive
                        ? 'bg-[#3f3f3f] text-white'
                        : 'text-[#aaaaaa] hover:bg-[#3f3f3f] hover:text-white'
                    }`
                  }
                >
                  Ideas
                  {ideasCount > 0 && (
                    <span className="bg-red-600 text-white text-xs px-1.5 py-0.5 rounded-full min-w-[20px] text-center">
                      {ideasCount > 99 ? '99+' : ideasCount}
                    </span>
                  )}
                </NavLink>
              </div>
            </div>
          </div>
        </nav>
      )
    }
    ```
  </action>
  <verify>
    TypeScript compiles: `npx tsc --noEmit`
    File exists at src/components/Nav.tsx
  </verify>
  <done>
    Navigation component created with NavLink, active states, and Ideas count badge
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Page Components</name>
  <files>
    src/pages/ChannelsPage.tsx
    src/pages/IdeasPage.tsx
  </files>
  <action>
    Create src/pages directory if not exists.

    Create src/pages/ChannelsPage.tsx (shell for now, will be expanded in Plan 05-07):
    ```typescript
    export function ChannelsPage() {
      return (
        <div className="max-w-7xl mx-auto p-4">
          <div className="flex items-center justify-between mb-6">
            <h1 className="text-2xl font-bold">Channels</h1>
            {/* AddChannelForm will go here */}
          </div>

          {/* ChannelGrid will go here */}
          <div className="text-[#aaaaaa]">
            <p>No channels tracked yet. Add a YouTube channel to get started.</p>
          </div>
        </div>
      )
    }
    ```

    Create src/pages/IdeasPage.tsx (placeholder for Phase 3):
    ```typescript
    export function IdeasPage() {
      return (
        <div className="max-w-7xl mx-auto p-4">
          <div className="flex items-center justify-between mb-6">
            <h1 className="text-2xl font-bold">Ideas</h1>
          </div>

          <div className="text-[#aaaaaa]">
            <p>No ideas saved yet. Browse channel videos and save ideas you like.</p>
          </div>
        </div>
      )
    }
    ```
  </action>
  <verify>
    TypeScript compiles: `npx tsc --noEmit`
    Both page files exist
  </verify>
  <done>
    ChannelsPage and IdeasPage shells created
  </done>
</task>

<task type="auto">
  <name>Task 3: Set Up React Router in App</name>
  <files>
    src/App.tsx
  </files>
  <action>
    Update src/App.tsx to include router and navigation:
    ```typescript
    import { BrowserRouter, Routes, Route } from 'react-router'
    import { Toaster } from 'sonner'
    import { Nav } from './components/Nav'
    import { ChannelsPage } from './pages/ChannelsPage'
    import { IdeasPage } from './pages/IdeasPage'

    export function App() {
      // TODO: Get actual ideas count from state/API in Phase 3
      const ideasCount = 0

      return (
        <BrowserRouter>
          <div className="min-h-screen bg-[#0f0f0f] text-[#f1f1f1]">
            <Nav ideasCount={ideasCount} />

            <main>
              <Routes>
                <Route path="/" element={<ChannelsPage />} />
                <Route path="/ideas" element={<IdeasPage />} />
              </Routes>
            </main>

            <Toaster position="bottom-right" theme="dark" richColors />
          </div>
        </BrowserRouter>
      )
    }
    ```

    Remove the Supabase connection test code from Plan 03 (it was temporary).
  </action>
  <verify>
    Run `npm run dev`
    Navigate to http://localhost:5173/ - should show Channels page
    Click "Ideas" in nav - URL changes to /ideas, Ideas page shows
    Click "Channels" - back to / with Channels page
    Active nav item has lighter background
  </verify>
  <done>
    Router working, navigation switches between pages, active state highlighted
  </done>
</task>

</tasks>

<verification>
1. Navigation bar visible at top of page
2. "Channels" link navigates to / and shows ChannelsPage
3. "Ideas" link navigates to /ideas and shows IdeasPage
4. Active page has highlighted background in nav (#3f3f3f)
5. Inactive pages have gray text (#aaaaaa)
6. Ideas badge shows (when count > 0)
7. No console errors
</verification>

<success_criteria>
- BrowserRouter configured in App.tsx
- Nav component with NavLink active states
- ChannelsPage renders at /
- IdeasPage renders at /ideas
- Dark theme consistent across all pages
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
