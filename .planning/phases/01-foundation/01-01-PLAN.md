---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - src/main.tsx
  - src/App.tsx
  - src/index.css
  - src/vite-env.d.ts
  - .env.example
  - .gitignore
  - index.html
autonomous: true
user_setup:
  - service: supabase
    why: "Database and real-time sync"
    env_vars:
      - name: VITE_SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> API -> Project URL"
      - name: VITE_SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> anon public key"

must_haves:
  truths:
    - "Dev server starts without errors"
    - "Browser shows dark-themed page"
    - "Hot reload works on file save"
    - "TypeScript compilation passes"
  artifacts:
    - path: "package.json"
      provides: "Dependencies and scripts"
      contains: "vite"
    - path: "src/index.css"
      provides: "Dark theme CSS variables"
      contains: "--color-background"
    - path: "src/App.tsx"
      provides: "Root component with Toaster"
      contains: "Toaster"
    - path: ".env.example"
      provides: "Environment variable template"
      contains: "VITE_SUPABASE"
  key_links:
    - from: "src/main.tsx"
      to: "src/App.tsx"
      via: "React render"
      pattern: "createRoot.*App"
    - from: "src/index.css"
      to: "tailwindcss"
      via: "import directive"
      pattern: "@import.*tailwindcss"
---

<objective>
Create the Vite + React + TypeScript + Tailwind CSS project scaffold with YouTube dark theme styling and toast notification system.

Purpose: Establishes the development environment and visual foundation that all other plans build upon.
Output: Working dev server with dark theme, hot reload, and toast notifications ready.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Vite React TypeScript Project</name>
  <files>
    package.json
    vite.config.ts
    tsconfig.json
    tsconfig.app.json
    tsconfig.node.json
    index.html
    src/main.tsx
    src/App.tsx
    src/vite-env.d.ts
    .gitignore
  </files>
  <action>
    Run `npm create vite@latest . -- --template react-ts` to scaffold in current directory.

    After scaffold, install dependencies:
    ```bash
    npm install @supabase/supabase-js sonner react-router
    npm install -D tailwindcss @tailwindcss/vite
    ```

    Update vite.config.ts to include Tailwind plugin:
    ```typescript
    import { defineConfig } from 'vite'
    import react from '@vitejs/plugin-react'
    import tailwindcss from '@tailwindcss/vite'

    export default defineConfig({
      plugins: [react(), tailwindcss()],
    })
    ```

    Update src/vite-env.d.ts with environment variable types:
    ```typescript
    /// <reference types="vite/client" />

    interface ImportMetaEnv {
      readonly VITE_SUPABASE_URL: string
      readonly VITE_SUPABASE_ANON_KEY: string
    }

    interface ImportMeta {
      readonly env: ImportMetaEnv
    }
    ```

    Create minimal src/App.tsx with Sonner Toaster:
    ```typescript
    import { Toaster } from 'sonner'

    export function App() {
      return (
        <div className="min-h-screen bg-[#0f0f0f] text-[#f1f1f1]">
          <main className="p-4">
            <h1 className="text-2xl font-bold">YouTube Competitor Tracker</h1>
            <p className="text-[#aaaaaa] mt-2">Foundation setup complete</p>
          </main>
          <Toaster position="bottom-right" theme="dark" richColors />
        </div>
      )
    }
    ```

    Update src/main.tsx to use named export:
    ```typescript
    import { StrictMode } from 'react'
    import { createRoot } from 'react-dom/client'
    import { App } from './App'
    import './index.css'

    createRoot(document.getElementById('root')!).render(
      <StrictMode>
        <App />
      </StrictMode>,
    )
    ```

    Delete the default App.css file (we use Tailwind instead).
  </action>
  <verify>
    Run `npm run dev` and confirm server starts on localhost:5173
    Run `npx tsc --noEmit` to verify TypeScript compiles
  </verify>
  <done>
    Dev server runs, TypeScript compiles, browser shows "YouTube Competitor Tracker" heading
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure Dark Theme and Environment</name>
  <files>
    src/index.css
    .env.example
    .env.local
  </files>
  <action>
    Replace src/index.css with YouTube dark theme configuration:
    ```css
    @import "tailwindcss";

    /* YouTube dark theme colors as CSS custom properties */
    :root {
      --color-background: #0f0f0f;
      --color-surface: #272727;
      --color-surface-hover: #3f3f3f;
      --color-text-primary: #f1f1f1;
      --color-text-secondary: #aaaaaa;
      --color-border: #3f3f3f;
      --color-accent: #ff0000;
    }

    /* Base dark theme applied globally */
    body {
      background-color: var(--color-background);
      color: var(--color-text-primary);
      font-family: 'Roboto', -apple-system, BlinkMacSystemFont, sans-serif;
    }

    /* Dialog backdrop styling */
    dialog::backdrop {
      background-color: rgba(0, 0, 0, 0.7);
    }

    /* Scrollbar styling for dark theme */
    ::-webkit-scrollbar {
      width: 8px;
    }

    ::-webkit-scrollbar-track {
      background: var(--color-background);
    }

    ::-webkit-scrollbar-thumb {
      background: var(--color-surface);
      border-radius: 4px;
    }

    ::-webkit-scrollbar-thumb:hover {
      background: var(--color-surface-hover);
    }
    ```

    Create .env.example with placeholder values:
    ```
    # Supabase Configuration
    # Get these from: Supabase Dashboard -> Project Settings -> API
    VITE_SUPABASE_URL=https://your-project-id.supabase.co
    VITE_SUPABASE_ANON_KEY=your-anon-key-here
    ```

    Create .env.local with same content (gitignored, for local dev).

    Ensure .gitignore includes:
    ```
    .env.local
    .env*.local
    ```
  </action>
  <verify>
    Refresh browser - page background should be #0f0f0f (very dark gray)
    Check .env.example exists with VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY placeholders
    Verify .env.local is gitignored: `git check-ignore .env.local` should return the path
  </verify>
  <done>
    Dark theme visible, environment template created, secrets gitignored
  </done>
</task>

</tasks>

<verification>
1. `npm run dev` starts server without errors
2. `npx tsc --noEmit` passes with no type errors
3. Browser at localhost:5173 shows dark background (#0f0f0f)
4. "YouTube Competitor Tracker" heading visible in white text
5. `.env.example` contains VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY
6. `.env.local` is gitignored
</verification>

<success_criteria>
- Dev server runs on localhost:5173
- Dark theme applied (background #0f0f0f, text #f1f1f1)
- TypeScript compiles without errors
- Sonner Toaster mounted (visible in React DevTools)
- Environment variable template documented
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
